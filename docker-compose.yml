version: "3.8"
services:
  main_container:
    build:
      context: .
      args:
        - CARLA_VERSION=${CARLA_VERSION}
      cache_from:
        - faizansana/intersection-driving:latest
    image: faizansana/intersection-driving:latest
    # ports:
    #   - "${GUI_TOOLS_VNC_PORT}:5900"
    environment:
      - VNC_PORT=${GUI_TOOLS_VNC_PORT}
    user: ${FIXUID}:${FIXGID}
    volumes:
      - ./:/home/docker/src
    depends_on:
      - carla_server

  carla_server:
    image: "carlasim/carla:${CARLA_VERSION}" 
    command: /bin/bash -c "SDL_VIDEODRIVER=offscreen && ./CarlaUE4.sh -RenderOffScreen -world-port=2000 -nosound -carla-server -fps=30"
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            device_ids: ['1']
            capabilities: [gpu]
    # ports:
    #   - "2000:2000"
